// Exc2.23.cpp : 此文件包含 "main" 函数。程序执行将在此处开始并结束。
/**
*	Read 5 integers, find out and print the maxium and minimum one
*	@author Linloir
*	@author Jonathan Zhang
*	@version 1.0 2020-10-13
*/

#include <iostream>
#include <stdio.h>

int main() {
	int integer1, integer2, integer3, integer4, integer5;	//int 5 integers

	puts("%s" , "Enter 5 different integers and seperate them with space: ");
	scanf_s("%d %d %d %d %d", &integer1， & integer2, &integer3, &integer4, &integer5);	//read the 5 integers

	int max, min;
	if (integer1 > integer2) {
		max = integer1;
		min = integer2;
	}
	else {
		max = integer2;
	}
	if (integer3 > max) {
		max = integer3;
	}
	else {
		if(integer)
	}
}

